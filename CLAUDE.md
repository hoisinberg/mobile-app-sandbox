# Chat App Project

See README.md in repository root for full implementation plan and progress.

**Current status:** Phase 1 complete. Next up is Phase 2: Firebase Auth.

## Quick Commands

```bash
# Rust server
cd rust-chat-server && cargo run

# React Native
cd chat-mobile && npm start

# Regenerate protos
cd chat-mobile && npm run proto:generate
```
